import { ExamplePipePipe } from './example-pipe.pipe';

describe('ExamplePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ExamplePipePipe();
    expect(pipe).toBeTruthy();
  });
});
